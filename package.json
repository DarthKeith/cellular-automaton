{
  "name": "cellular-automaton",
  "version": "0.0.1",
  "description": "A web application for exploring cellular automata governed by randomly generated iteration rules.",
  "author": "Keith Kaplan",
  "type": "module",
  "scripts": {
    "build": "spago build && node build.js",
    "start": "parcel docs/index.html --open",
    "build-dev": "spago build && parcel build dev/index.html",
    "serve-dev": "npm run build-dev && parcel dev/index.html --open"
  },
  "devDependencies": {
    "esbuild": "0.23.0",
    "parcel": "^2.12.0"
  }
}
